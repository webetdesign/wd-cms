<link rel="canonical" href="{{ absolute_url(canonical.path) }}"/>
{% for alternate in alternates %}
    <link rel="alternate" href="{{ absolute_url(alternate.path) }}" hreflang="{{ alternate.locale }}"/>
{% endfor %}
